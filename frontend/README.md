# Frontend

TODO:
- Convert this skeleton into a React 18 + Vite application.
- Add package metadata, routing, layout, pages, stores, API client, and reusable components.
- Connect frontend actions to the Laravel API through `VITE_API_BASE_URL`.
