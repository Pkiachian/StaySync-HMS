# Storage

TODO:
- Store generated invoices, uploads, exports, and temporary files here once Laravel storage is configured.
- Do not commit generated runtime files.
- Use `.gitkeep` only to keep this folder visible in the project skeleton.
