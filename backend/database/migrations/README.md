# Migrations

TODO:
- Add schema migrations for all StaySync HMS tables listed in `requirements.txt`.
- Create migrations in dependency order so foreign keys apply cleanly.
- Add indexes for room availability, booking date ranges, status filters, and report queries.
