# Eloquent Models

TODO:
- Add models for users, guests, room types, rooms, bookings, folio charges, payments, housekeeping tasks, room status logs, rate overrides, settings, and booking addons.
- Define relationships, fillable fields, casts, and common query scopes.
- Keep business logic in services when it spans multiple models.
