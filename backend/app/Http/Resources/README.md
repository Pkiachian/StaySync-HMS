# API Resources

TODO:
- Add JSON resource classes that shape API responses consistently.
- Hide internal database fields and expose only frontend-ready values.
- Include related model summaries only when needed by each endpoint.
