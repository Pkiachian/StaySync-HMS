# Backend

TODO:
- Convert this skeleton into a Laravel 10 API application.
- Add Composer dependencies, environment files, migrations, models, services, controllers, routes, seeders, and tests.
- Keep business logic in services and expose JSON responses through API resources.
